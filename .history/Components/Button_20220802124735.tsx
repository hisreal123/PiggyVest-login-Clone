import React from 'react'

export const Button: React.FC = () => {
  return (
    <div>
        <button type={type} class name {
            constructor(parameters) {
                
            }
        }>{content} </button>
    </div>
  )
}
